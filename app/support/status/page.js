import CheckTicketStatus from '../../components/check-ticket-status';

export const metadata = {
  title: 'Check Ticket Status | Support Center | Sinetcom',
  description: 'Check the status of your support ticket by ticket number and email.',
};

export default function CheckStatusPage() {
  return <CheckTicketStatus />;
}
